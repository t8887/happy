// Web stub for @react-native-community/datetimepicker
// This package has no web implementation. Since all DateTimePicker usage
// in this app is guarded by Platform.OS checks (ios/android only), this
// stub will never actually render — it just prevents Metro from crashing
// on web builds.
import React from 'react';
import { View } from 'react-native';

const DateTimePicker = () => <View />;
export default DateTimePicker;
